This is the deployment version of phandango.


To deploy:
* on `master`, bump version number, build, commit and push.
* on `gh-pages`, run `git checkout master -- dist/phandango.js`, commit and push.
